{
    "name": "test",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "build": "tsc --build",
        "clean": "rimraf dist/",
        "build:clean": "yarn clean && tsc --build",
        "deploy:dev": "yarn build:clean && sls deploy"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "aws-sdk": "^2.1047.0"
    },
    "devDependencies": {
        "@types/node": "^18.11.18",
        "bluebird": "^3.7.2",
        "rimraf": "^3.0.2",
        "serverless-secret-baker": "^2.1.0",
        "typescript": "^4.9.4"
    }
}